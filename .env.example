# Key NAMES only — never values (CON-9: secrets are environment-only,
# config.toml rejects key-like values at load).
# Copy to .env (gitignored) or export in your shell profile.

# Council providers (spec 05)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# Optional config overrides (full registry: docs/config.md)
# LOCALAI_MEM_CEILING_GB=22
# LOCALAI_INFERENCE_PORT=8080
# LOCALAI_QUEUE_PERMITS=3
